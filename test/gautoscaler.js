var gautoscaler = require("../lib/gautoscaler");

describe("gautoscaler", function() {

    describe("user record an event", function() {

        it("shouldn't add events with same date (start and end)", function(done) {
            done();
        })
    });
});









